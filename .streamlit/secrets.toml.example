# Copy this file to .streamlit/secrets.toml locally (never commit the real one),
# or paste its contents into your app's "Secrets" settings on Streamlit Community Cloud.

GOOGLE_MAPS_API_KEY = "your-key-here"
